<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="16dp">


    <ImageView
        android:id="@+id/mIVCastAvatar"
        android:layout_width="70dp"
        android:layout_height="100dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:src="@drawable/x" />

    <TextView
        android:id="@+id/mTVCastName"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginEnd="16dp"
        android:layout_marginStart="16dp"
        android:layout_marginTop="16dp"
        android:fontFamily="@font/archivo_black"
        android:textSize="16sp"
        app:layout_constraintStart_toEndOf="@+id/mIVCastAvatar"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="Oscar" />

    <TextView
        android:id="@+id/mTVCastCharacter"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:fontFamily="@font/roboto"
        android:layout_marginEnd="16dp"
        android:layout_marginStart="16dp"
        android:textSize="14sp"
        app:layout_constraintStart_toEndOf="@+id/mIVCastAvatar"
        app:layout_constraintTop_toBottomOf="@+id/mTVCastName"
        tools:text="Oscar" />

</android.support.constraint.ConstraintLayout>
